'use server';

/**
 * @fileOverview DECOMMISSIONED PROTOCOL
 * All Chat Studio server actions have been purged to ensure registry stability.
 */

export async function uploadAvatarAction() {
  return { success: false, error: 'Protocol decommissioned.' };
}
